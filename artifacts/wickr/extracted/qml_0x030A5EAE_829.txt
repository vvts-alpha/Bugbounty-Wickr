<import QtQuick
import QtQuick.Window
import com.wickr.widgets 1.0
import "../Commons/"
import QtQuick.Controls.Basic

QtObject {
    id: root
    property bool visible: false
    property int foreignWindowId: 0

    property variant timer: Timer {
        interval: 100
        running: true
        repeat: true
        onTriggered: {
            root.overlay.updatePosition();
        }
    }

    property var overlay: ForeignWindowOverlay {
        id: overlay
        visible: root.visible
        foreignWindowId: root.foreignWindowId
        color: "transparent"

        Rectangle {
            id: border
            anchors.fill: parent
            color: "transparent"
            border.width: 4 * ScreenRM.scale
            border.color: Branding.palette_gbl_s1
        }
    }
}
